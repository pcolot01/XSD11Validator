<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <artifactId>QualityControlReports</artifactId>
    <groupId>lu.infeurope</groupId>
    <version>1.3</version>
    <relativePath>../QualityControlReports/pom.xml</relativePath>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>eu.europa.publications.ifc.cov</groupId>
  <artifactId>XSD11Validator</artifactId>
  <name>Xerces validator</name>
  <version>2.0-SNAPSHOT</version>
  <build>
    <finalName>maven-xerces</finalName>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>build-helper-maven-plugin</artifactId>
          <version>${build-helper-maven-plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${maven-shade-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>xml-maven-plugin</artifactId>
          <version>${xml-maven-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>xml-maven-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-test-sources</phase>
            <goals>
              <goal>transform</goal>
            </goals>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>net.sourceforge.saxon</groupId>
            <artifactId>saxon</artifactId>
            <version>9.1.0.8</version>
          </dependency>
        </dependencies>
        <configuration>
          <skip>false</skip>
          <transformationSets>
            <transformationSet>
              <dir>src/test/resources/eu/europa/publications/xml/testSuites/</dir>
              <includes>
                <include>suite.xml</include>
              </includes>
              <stylesheet>src/test/xslt/testSuiteGenerator.xsl</stylesheet>
            </transformationSet>
          </transformationSets>
          <catalogs>
            <catalog>src/test/resources/eu/europa/publications/xml/common/catalog.xml</catalog>
          </catalogs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>add-source</id>
            <phase>generate-test-sources</phase>
            <goals>
              <goal>add-test-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>src/main/java/</source>
                <source>target/generated-test-sources/java/</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>${project.build.sourceVersion}</source>
          <target>${project.build.targetVersion}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>module-info.class</exclude>
                    <exclude>META-INF/DEPENDENCIES</exclude>
                    <exclude>META-INF/MANIFEST.MF</exclude>
                  </excludes>
                </filter>
              </filters>
              <shadedArtifactAttached>true</shadedArtifactAttached>
              <transformers>
                <transformer />
                <transformer>
                  <addHeader>false</addHeader>
                </transformer>
                <transformer>
                  <manifestEntries>
                    <Main-Class>eu.europa.publications.xml.tools.Main</Main-Class>
                    <Multi-Release>true</Multi-Release>
                  </manifestEntries>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-Dhttps.proxyHost=${env.HTTPS.PROXYHOST} -Dhttps.proxyPort=${env.HTTPS.PROXYPORT} -Dhttp.proxyHost=${env.HTTP.PROXYHOST} -Dhttp.proxyPort=${env.HTTP.PROXYPORT} -Dhttp.nonProxyHosts='${env.HTTP.NONPROXYHOSTS}' -Dhttp.proxyUser=${env.HTTP.PROXYUSER} -Dhttp.proxyPassword=${env.HTTP.PROXYPASSWORD}
						-Djdk.http.auth.tunneling.disabledSchemes=${env.JDK.HTTP.AUTH.TUNNELING.DISABLEDSCHEMES}
						-Djdk.https.auth.tunneling.disabledSchemes=${env.JDK.HTTPS.AUTH.TUNNELING.DISABLEDSCHEMES}</argLine>
          <excludes>
            <exclude>**/__selectionPattern__.java</exclude>
          </excludes>
          <systemPropertyVariables>
            <log4j.configurationFile>./src/test/resources/log4j2.xml</log4j.configurationFile>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <version>5.10.1</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>opentest4j</artifactId>
          <groupId>org.opentest4j</groupId>
        </exclusion>
        <exclusion>
          <artifactId>junit-platform-commons</artifactId>
          <groupId>org.junit.platform</groupId>
        </exclusion>
        <exclusion>
          <artifactId>apiguardian-api</artifactId>
          <groupId>org.apiguardian</groupId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>5.10.1</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>junit-platform-engine</artifactId>
          <groupId>org.junit.platform</groupId>
        </exclusion>
        <exclusion>
          <artifactId>apiguardian-api</artifactId>
          <groupId>org.apiguardian</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
  <properties>
    <xml-maven-plugin.version>1.1.0</xml-maven-plugin.version>
    <xpath2.version>1.2.0</xpath2.version>
    <log4j.version>2.22.1</log4j.version>
    <commons-cli.version>1.6.0</commons-cli.version>
    <xml-resolver.version>1.2</xml-resolver.version>
    <maven-shade-plugin.version>3.5.1</maven-shade-plugin.version>
    <build-helper-maven-plugin.version>3.5.0</build-helper-maven-plugin.version>
    <junit-jupiter-api.version>5.10.1</junit-jupiter-api.version>
    <xml-apis.version>1.4.01</xml-apis.version>
    <xercesImpl.version>2.12.2</xercesImpl.version>
    <junit-jupiter-engine.version>5.10.1</junit-jupiter-engine.version>
    <java-cup.version>10k</java-cup.version>
  </properties>
</project>
